% ===========================================================================
% ANN Pthread + OpenMP 并行优化实验报告
%
% 数据路径:
%   本机 Windows: ../files/  (D:\Study\26sp\parallel\files\)
%   鲲鹏服务器:   /anndata/
%
% 实验结果:
%   本地: results/local/  → 汇总 results/local_summary.csv + local_tradeoff.csv
%   鲲鹏: 待跑
%
% 编译: xelatex main.tex
% ===========================================================================
\documentclass[a4paper,11pt,UTF8]{ctexart}

\usepackage{fontspec}
\usepackage[a4paper,left=2.35cm,right=2.35cm,top=2.45cm,bottom=2.45cm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{float}
\usepackage{graphicx}
\usepackage[pdfpagelabels=false,pageanchor=false]{hyperref}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{array}
\usepackage{longtable}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit}

\graphicspath{{./}{fig/}}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=black}
\linespread{1.10}
\renewcommand{\today}{\number\year 年 \number\month 月 \number\day 日}
\renewcommand{\contentsname}{目录}
\renewcommand{\figurename}{图}
\renewcommand{\tablename}{表}
\captionsetup{font=small,labelsep=quad}
\setlist{leftmargin=1.8em,itemsep=2pt,topsep=3pt}
\setlength{\parindent}{2em}
\setlength{\parskip}{2pt}
\setlength{\textfloatsep}{10pt plus 2pt minus 2pt}
\setcounter{tocdepth}{2}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue!70!black},
  commentstyle=\color{green!40!black},
  numbers=left,
  numberstyle=\tiny,
  frame=single,
  breaklines=true,
  aboveskip=6pt,
  belowskip=6pt
}

\pagestyle{fancy}
\fancyhf{}
\lhead{\kaishu ANN 近似最近邻搜索}
\rhead{\kaishu Pthread / OpenMP 并行优化实验报告}
\cfoot{\thepage}
\setlength{\headheight}{14pt}
\renewcommand{\headrulewidth}{0.1pt}

\begin{document}

\begin{titlepage}
  \begin{center}
    \includegraphics[width=0.8\textwidth]{NKU.png}\\[1cm]
    \vspace{18mm}
    {\huge\kaishu 计算机学院\par}
    \vspace{0.5cm}
    {\huge\kaishu 并行程序设计期末报告\par}
    \vspace{2.0cm}
    {\Huge\kaishu Lab3 Pthread + OpenMP 编程\par}
    \vspace{0.35cm}
    {\Huge\kaishu ANN 近似最近邻搜索\par}
    \vspace{0.55cm}
    {\Large\kaishu Flat / SQ / PQ / FastScan / IVF / IVF-PQ / HNSW\par}
    \vspace{0.3cm}
    {\Large\kaishu 多线程并行化与调度策略对比\par}

    \vspace{\fill}
    {\LARGE\kaishu 姓名：柯云超\par}
    \vspace{0.5cm}
    {\LARGE\kaishu 学号：2413575\par}
    \vspace{0.5cm}
    {\LARGE\kaishu 专业：计算机科学与技术\par}

    \vfill
    {\Large \today\par}
  \end{center}
\end{titlepage}

\tableofcontents
\clearpage

\section*{摘要}
\addcontentsline{toc}{section}{摘要}

本实验围绕 DEEP100K 上的近似最近邻搜索（ANNS）实现了一个 header-only 的多算法并行框架。系统以 SIMD 距离计算为底层公共内核，在此基础上实现 Flat-SIMD、SQ-SIMD、PQ-SIMD、PQ-FastScan、IVF-SIMD、IVF-PQ-SIMD 与 HNSW 图索引，并分别给出 Pthread 与 OpenMP 版本。Pthread 侧实现了 static、dynamic 与 pool 三类调度；并行粒度覆盖 query 级并行（inter-query）与单 query 内部的 base/list 划分（intra-query）。实验在本机 i9-13900H 平台完成 278 组核心矩阵结果，并补充 15 组 IVF / IVF-PQ 的 \texttt{nprobe} 与构建模式 trade-off 结果。

实验结果显示，query 级并行是本数据集和批量查询场景下最稳健的并行粒度。Flat、SQ、PQ、FastScan、IVF 与 IVF-PQ 的最优点均来自 inter-query；其中 Flat、SQ、PQ、FastScan、IVF 与 IVF-PQ 的本地最优延迟分别为 298.95、86.53、73.06、125.81、104.67 与 27.44 $\mu$s。Pthread dynamic 在多数规则扫描类算法上表现最好，Pthread pool 在 IVF inter-query 上略优，说明线程创建开销、尾部不均衡与 inverted list 不均匀性都会影响最终表现。HNSW 的内部并行更复杂：原始 baseline 延迟最低但召回较低，多入口点和边划分能提升召回，但同步和路径依赖使其不适合作为默认最快路径。

\noindent\textbf{关键词：} ANN，Pthread，OpenMP，SIMD，IVF，IVF-PQ，HNSW，FastScan，负载均衡

\section{问题定义与目标}

给定基向量集合
\[
X=\{x_1,x_2,\ldots,x_N\}\subset\mathbb{R}^d
\]
以及查询向量 $q\in\mathbb{R}^d$，$k$ 近邻搜索希望找到 $X$ 中与 $q$ 距离最近的 $k$ 个向量。本文使用的距离形式沿用课程框架中的内积距离：
\[
\delta(x,q)=1-\sum_{i=1}^{d}x_iq_i .
\]
近似最近邻搜索不要求每次完全返回真实 top-$k$，因此用 Recall@$k$ 衡量准确率：
\[
\mathrm{Recall@}k = \frac{|KNN(q)\cap KANN(q)|}{k}.
\]

本次作业的核心目标不是单独做 SIMD，而是在已经具备 SIMD 距离内核的基础上，系统实现 Pthread / OpenMP 多线程并行，并从以下方面进行评估：
\begin{enumerate}
  \item 对 Flat、SQ、PQ、FastScan、IVF、IVF-PQ 与 HNSW 进行统一实现和测试；
  \item 比较 Pthread static、dynamic、pool 与 OpenMP 的任务划分和调度开销；
  \item 比较 inter-query 与 intra-query 两种并行粒度；
  \item 分析线程数、负载均衡、top-$k$ reduce、cache / 内存访问和负优化现象；
  \item 对 IVF / IVF-PQ 给出 \texttt{nprobe} 与构建模式对 latency-recall 的影响。
\end{enumerate}

\section{系统设计}

\subsection{目录与模块}

实现按功能拆分为公共 SIMD 内核、Pthread 并行层、OpenMP 并行层、IVF 系列、HNSW 系列和独立 main 程序。所有新增算法均采用 header-only 方式，满足课程框架只编译 \texttt{main.cc} 的约束。

\begin{table}[H]
  \centering\small
  \begin{tabular}{p{3.0cm}p{9.6cm}}
    \toprule
    模块 & 作用 \\
    \midrule
    \path{simd/} & 统一存放 Flat、SQ、PQ、FastScan 的 SIMD 距离计算和查表扫描内核，例如 \path{flat_scan_simd.h}、\path{pq_fastscan_simd.h}。 \\
    \path{pthread/} & Flat、SQ、PQ、FastScan 的 Pthread 并行实现，以及公共线程池 \path{thread_pool.h}。 \\
    \path{omp/} & Flat、SQ、PQ、FastScan 的 OpenMP 并行实现。 \\
    \path{ivf/} & IVF 索引、IVF-SIMD、IVF-PQ 以及对应 Pthread / OpenMP 并行搜索。 \\
    \path{hnsw/} & 基于 hnswlib 的多入口点、边划分、Layer0 划分与 IVF-HNSW 嵌套搜索。 \\
    \path{mains/} & 71 个可独立编译的实验入口，用于不同算法、调度策略和并行粒度的测试。 \\
    \path{results/local/} & 本地实验原始输出文件。每个文件包含 label、Recall@10 与平均查询延迟。 \\
    \bottomrule
  \end{tabular}
  \caption{代码模块划分}
  \label{tab:modules}
\end{table}

\subsection{并行框架}

本文实现了两类并行粒度。

\textbf{Inter-query 并行。} 每个线程处理一批完整 query。线程间不共享搜索状态，只写入结果数组中的不同位置，因此同步开销最低。对于批量查询，inter-query 通常能获得最好的吞吐和延迟表现。

\textbf{Intra-query 并行。} 对单条 query，将 base 向量集合、候选集合或 IVF inverted list 分给多个线程。每个线程维护局部 top-$k$ 或 top-$p$ 堆，最后由主线程 merge。这种方式适合单 query 延迟优化，但会引入局部堆维护、线程同步和 top-$k$ reduce 成本。

Pthread 侧实现三种调度方式：
\begin{itemize}
  \item static：静态等分任务，适合 Flat 这类计算量规则的扫描；
  \item dynamic：用原子计数器动态抢任务，适合 query 耗时或 list 长度不均匀的场景；
  \item pool：预创建线程池和任务队列，减少反复创建 / 销毁线程的开销。
\end{itemize}

OpenMP 侧主要使用 \texttt{parallel for}，query 级循环使用 dynamic schedule，base/list 扫描使用局部堆加串行 merge。

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    scale=0.82,
    transform shape,
    node distance=1.0cm,
    box/.style={draw, rounded corners=2pt, align=center, minimum width=2.8cm, minimum height=0.8cm},
    arrow/.style={-Latex, thick}
  ]
    \node[box] (data) {DEEP100K\\base/query/gt};
    \node[box, right=0.8cm of data] (index) {离线索引\\SQ/PQ/IVF/HNSW};
    \node[box, right=0.8cm of index] (sched) {并行调度\\Pthread / OpenMP};
    \node[box, below=1.0cm of sched] (simd) {SIMD 距离内核\\IP / L2 / ADC};
    \node[box, right=0.8cm of sched] (topk) {局部 top-$k$\\merge};
    \node[box, right=0.8cm of topk] (eval) {Recall@10\\Latency};
    \draw[arrow] (data) -- (index);
    \draw[arrow] (index) -- (sched);
    \draw[arrow] (sched) -- (topk);
    \draw[arrow] (topk) -- (eval);
    \draw[arrow] (simd) -- (sched);
  \end{tikzpicture}
  \caption{ANN 多线程并行框架}
  \label{fig:framework}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    scale=0.85,
    transform shape,
    box/.style={draw, rounded corners=1.5pt, align=center, minimum width=2.4cm, minimum height=0.65cm, font=\footnotesize},
    tbox/.style={draw, fill=blue!8, rounded corners=1.5pt, align=center, minimum width=1.5cm, minimum height=0.55cm, font=\footnotesize},
    arrow/.style={-Latex, thick}
  ]
    % Inter-query
    \node[box, fill=green!5] (inter_title) at (0, -0.2) {\textbf{Inter-query 并行}};
    \node[tbox] (q1) at (-2.5, -1.3) {$q_0$};
    \node[tbox] (q2) at (-0.8, -1.3) {$q_1$};
    \node[tbox] (q3) at (0.9, -1.3) {$q_2$};
    \node[tbox] (q4) at (2.6, -1.3) {$q_3$};
    \node[tbox] (t1) at (-2.5, -2.2) {线程 0};
    \node[tbox] (t2) at (-0.8, -2.2) {线程 1};
    \node[tbox] (t3) at (0.9, -2.2) {线程 2};
    \node[tbox] (t4) at (2.6, -2.2) {线程 3};
    \node[align=center, font=\footnotesize] (r1) at (-2.5, -3.0) {完整搜索\\top-$k$};
    \node[align=center, font=\footnotesize] (r2) at (-0.8, -3.0) {完整搜索\\top-$k$};
    \node[align=center, font=\footnotesize] (r3) at (0.9, -3.0) {完整搜索\\top-$k$};
    \node[align=center, font=\footnotesize] (r4) at (2.6, -3.0) {完整搜索\\top-$k$};
    \draw[arrow] (q1) -- (t1); \draw[arrow] (t1) -- (r1);
    \draw[arrow] (q2) -- (t2); \draw[arrow] (t2) -- (r2);
    \draw[arrow] (q3) -- (t3); \draw[arrow] (t3) -- (r3);
    \draw[arrow] (q4) -- (t4); \draw[arrow] (t4) -- (r4);

    % Intra-query
    \node[box, fill=orange!5] (intra_title) at (7.5, -0.2) {\textbf{Intra-query 并行}};
    \node[tbox, minimum width=2.8cm] (query) at (7.5, -1.3) {单条 query $q$};
    \node[tbox] (b1) at (5.0, -2.2) {base[0:N/4)};
    \node[tbox] (b2) at (6.7, -2.2) {base[N/4:N/2)};
    \node[tbox] (b3) at (8.4, -2.2) {base[N/2:3N/4)};
    \node[tbox] (b4) at (10.1, -2.2) {base[3N/4:N)};
    \node[tbox] (w1) at (5.0, -3.0) {线程 0};
    \node[tbox] (w2) at (6.7, -3.0) {线程 1};
    \node[tbox] (w3) at (8.4, -3.0) {线程 2};
    \node[tbox] (w4) at (10.1, -3.0) {线程 3};
    \node[align=center, font=\footnotesize] (h1) at (5.0, -3.8) {局部 heap};
    \node[align=center, font=\footnotesize] (h2) at (6.7, -3.8) {局部 heap};
    \node[align=center, font=\footnotesize] (h3) at (8.4, -3.8) {局部 heap};
    \node[align=center, font=\footnotesize] (h4) at (10.1, -3.8) {局部 heap};
    \node[box, fill=yellow!5] (merge) at (7.5, -4.7) {merge → 全局 top-$k$};
    \draw[arrow] (query) -- (b1); \draw[arrow] (query) -- (b2);
    \draw[arrow] (query) -- (b3); \draw[arrow] (query) -- (b4);
    \draw[arrow] (b1) -- (w1); \draw[arrow] (w1) -- (h1);
    \draw[arrow] (b2) -- (w2); \draw[arrow] (w2) -- (h2);
    \draw[arrow] (b3) -- (w3); \draw[arrow] (w3) -- (h3);
    \draw[arrow] (b4) -- (w4); \draw[arrow] (w4) -- (h4);
    \draw[arrow] (h1) -- (merge); \draw[arrow] (h2) -- (merge);
    \draw[arrow] (h3) -- (merge); \draw[arrow] (h4) -- (merge);
  \end{tikzpicture}
  \caption{Inter-query 与 Intra-query 并行粒度对比}
  \label{fig:granularity}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    scale=0.85,
    transform shape,
    box/.style={draw, rounded corners=1.5pt, align=center, minimum width=1.8cm, minimum height=0.6cm, font=\footnotesize},
    task/.style={draw, fill=blue!5, minimum width=0.7cm, minimum height=0.5cm, font=\tiny},
    arrow/.style={-Latex, thick}
  ]
    % Static
    \node[box, fill=green!5] (stitle) at (0, 0) {Static 静态划分};
    \foreach \i in {0,...,7} {
      \node[task] (s\i) at ({-2.1+\i*0.6}, -0.9) {\tiny T\i};
    }
    \node[draw, dashed, fit=(s0)(s1), inner sep=1pt] (sg0) {};
    \node[draw, dashed, fit=(s2)(s3), inner sep=1pt] (sg1) {};
    \node[draw, dashed, fit=(s4)(s5), inner sep=1pt] (sg2) {};
    \node[draw, dashed, fit=(s6)(s7), inner sep=1pt] (sg3) {};
    \node[font=\footnotesize] at (sg0.south) [yshift=-0.7cm] {线程 0};
    \node[font=\footnotesize] at (sg1.south) [yshift=-0.7cm] {线程 1};
    \node[font=\footnotesize] at (sg2.south) [yshift=-0.7cm] {线程 2};
    \node[font=\footnotesize] at (sg3.south) [yshift=-0.7cm] {线程 3};
    \draw[arrow] (sg0.south) -- ++(0,-0.35); \draw[arrow] (sg1.south) -- ++(0,-0.35);
    \draw[arrow] (sg2.south) -- ++(0,-0.35); \draw[arrow] (sg3.south) -- ++(0,-0.35);

    % Dynamic
    \node[box, fill=orange!5] (dtitle) at (5.0, 0) {Dynamic 原子抢夺};
    \node[task, fill=red!5] (counter) at (5.0, -0.9) {\texttt{atomic} 计数器};
    \foreach \i in {0,...,7} {
      \node[task] (d\i) at ({2.9+\i*0.6}, -1.7) {\tiny T\i};
    }
    \node[font=\footnotesize] (dw1) at (3.5, -2.5) {线程 0};
    \node[font=\footnotesize] (dw2) at (4.7, -2.5) {线程 1};
    \node[font=\footnotesize] (dw3) at (5.9, -2.5) {线程 2};
    \node[font=\footnotesize] (dw4) at (7.1, -2.5) {线程 3};
    \draw[arrow] (counter) -- (d0); \draw[arrow] (counter) -- (d2);
    \draw[arrow] (counter) -- (d4); \draw[arrow] (counter) -- (d6);
    \draw[arrow] (d0) -- (dw1); \draw[arrow] (d1) -- (dw2);
    \draw[arrow] (d3) -- (dw3); \draw[arrow] (d5) -- (dw4);

    % Pool
    \node[box, fill=blue!5] (ptitle) at (10.0, 0) {Pool 线程池};
    \node[task, fill=cyan!5, minimum width=2.2cm] (queue) at (10.0, -0.9) {\texttt{mutex+cond} 任务队列};
    \node[task, minimum width=1.0cm] (pw1) at (8.2, -1.8) {线程 0};
    \node[task, minimum width=1.0cm] (pw2) at (9.4, -1.8) {线程 1};
    \node[task, minimum width=1.0cm] (pw3) at (10.6, -1.8) {线程 2};
    \node[task, minimum width=1.0cm] (pw4) at (11.8, -1.8) {线程 3};
    \draw[arrow, dashed] (queue) -- (pw1); \draw[arrow, dashed] (queue) -- (pw2);
    \draw[arrow, dashed] (queue) -- (pw3); \draw[arrow, dashed] (queue) -- (pw4);
  \end{tikzpicture}
  \caption{Pthread 三种调度方式对比}
  \label{fig:schedulers}
\end{figure}

\section{算法实现}

\subsection{Flat-SIMD}

Flat 是精确全量扫描。每个 query 需要访问全部 $N$ 个 base 向量并维护 top-$k$ 堆。距离计算复用 \path{simd/flat_scan_simd.h}，在 x86 上使用 AVX2，在 ARM 上保留 NEON 路径。

Flat 的计算量规则，因此理论上 static 划分也应较强。但在本机 Windows 混合核调度环境下，dynamic query 抢任务可以减轻尾部等待和系统噪声，实际取得最优结果。Intra-query 版本需要每个线程维护局部堆并在单 query 结束后 merge，因此固定开销显著。

\subsection{SQ-SIMD}

SQ 将 float32 向量逐维量化为 uint8，查询阶段采用两阶段流程：第一阶段在 uint8 空间快速粗排得到 top-$p$ 候选，第二阶段对候选使用原始 float32 内积进行 rerank。本文在 Pthread 与 OpenMP 中都实现了 query 级整条流水线并行，以及单 query 内粗排阶段 base 划分并行。由于精排候选集合较小，精排阶段主要保持单线程执行，避免额外同步开销。

\subsection{PQ-SIMD 与 FastScan}

PQ 将 96 维向量切分为 $M=8$ 个子空间，每个子空间训练 256 个中心。查询时对每个子空间构建 LUT，并用 ADC 对 base 的 PQ code 查表累加。Pthread / OpenMP 版本分别对 query 循环或 base code 扫描进行并行化。

FastScan 是 PQ 的进一步压缩实现，使用 4-bit code 和更小 LUT，将查表过程转为寄存器内 shuffle。FastScan 的 ADC 阶段更偏 memory-bound，线程数过多可能更早触及带宽上限。因此本文重点比较 inter-query 与 intra-query 的差异，并在报告中保留负优化结果。

\subsection{IVF-SIMD}

IVF 在离线阶段用 KMeans 将 base 聚为 \texttt{nlist} 个簇。查询时先计算 query 到各簇中心的距离，选择最近的 \texttt{nprobe} 个簇，再扫描这些 inverted list 并用 SIMD 原始距离 rerank。实现中保留簇内连续存储信息，减少精排阶段的随机访问。

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    scale=0.88,
    transform shape,
    box/.style={draw, rounded corners=2pt, align=center, minimum width=2.4cm, minimum height=0.7cm, font=\footnotesize},
    sbox/.style={draw, fill=blue!3, rounded corners=1.5pt, align=center, minimum width=1.6cm, minimum height=0.55cm, font=\footnotesize},
    arrow/.style={-Latex, thick}
  ]
    \node[box, fill=green!5] (query) at (0, 0) {查询向量 $q$};
    \node[box, fill=yellow!5] (coarse) at (3.5, 0) {粗排阶段\\计算 $q$ 到 $K$ 个质心距离};
    \node[box, fill=orange!5] (nprobe) at (7.0, 0) {取 top-\texttt{nprobe}\\选择候选簇};
    \node[box, fill=blue!5] (fine) at (10.5, 0) {精排阶段\\SIMD 扫描 inverted lists};
    \node[box, fill=red!5] (rerank) at (14.0, 0) {top-$k$ 堆\\输出结果};

    \draw[arrow] (query) -- (coarse);
    \draw[arrow] (coarse) -- (nprobe);
    \draw[arrow] (nprobe) -- (fine);
    \draw[arrow] (fine) -- (rerank);

    \node[sbox] (c1) at (3.5, -1.3) {簇 0};
    \node[sbox] (c2) at (5.2, -1.3) {簇 1};
    \node[sbox] (c3) at (6.9, -1.3) {簇 2};
    \node[sbox, draw=red!70, thick] (c4) at (8.6, -1.3) {簇 3 $\checkmark$};
    \node[sbox] (ck) at (10.5, -1.3) {$\cdots$};

    \draw[arrow, gray] (coarse) -- (c1); \draw[arrow, gray] (coarse) -- (c2);
    \draw[arrow, gray] (coarse) -- (c3); \draw[arrow, red!70] (coarse) -- (c4);
    \draw[arrow, gray] (coarse) -- (ck);

    \node[align=center, font=\footnotesize, text width=4cm] at (10.5, -2.0) {扫描选中簇内向量\\$\delta(x,q)=1-\sum x_iq_i$ (SIMD)};
    \draw[arrow, red!70] (c4) -- (fine);
  \end{tikzpicture}
  \caption{IVF 两阶段搜索流程}
  \label{fig:ivf_pipeline}
\end{figure}

IVF 粗排计算量小，真正适合并行的是精排阶段。由于不同 inverted list 长度不均匀，dynamic 和 pool 调度更有意义。本文实现：
\begin{itemize}
  \item IVF-SIMD baseline；
  \item query 级 Pthread / OpenMP 并行；
  \item 单 query 内按 list 或候选段划分并行；
  \item \texttt{nprobe} sweep，用于展示 latency-recall trade-off。
\end{itemize}

\subsection{IVF-PQ-SIMD}

IVF-PQ 将 IVF 的候选缩减与 PQ 的压缩距离计算叠加。本文实现两种构建方式：
\begin{enumerate}
  \item global-PQ-first：先对所有 base 做全局 PQ 编码，再构建 IVF；
  \item IVF-local-PQ：先构建 IVF，再在簇内维护局部 PQ 编码。
\end{enumerate}

查询时先 IVF 粗排选择 \texttt{nprobe} 个簇，再在选中簇内用 PQ ADC 得到候选并 rerank。由于 PQ 显著减少单次距离计算量，IVF-PQ 更容易转为内存访问和调度开销主导，线程数并非越多越好。

\subsection{HNSW 进阶}

HNSW 基于 hnswlib 实现。本文不修改 hnswlib 源码，而是通过封装访问底层图结构，实现以下并行策略：
\begin{itemize}
  \item baseline：原始 \texttt{searchKnn}；
  \item 多入口点并行：多个线程从不同入口出发，最后 merge top-$k$；
  \item 边划分并行：并行计算当前节点邻居距离；
  \item Layer0 点划分：按节点 ID 范围切分底层图扫描；
  \item IVF-HNSW 嵌套：先 IVF 粗排，再并行搜索多个小 HNSW。
\end{itemize}
图搜索路径依赖强，内部并行容易被同步和候选队列更新开销抵消，因此 HNSW 部分重点如实分析正优化与负优化。

\section{实验设计}

\subsection{平台与数据集}

本地实验平台为 Intel Core i9-13900H，14 核 20 逻辑线程；编译器为 MSYS2 GCC 14.2.0。核心编译选项为：
\[
\texttt{-O2 -mavx2 -mfma -fopenmp -lpthread -std=c++17 -I.}
\]
数据集为 DEEP100K：100K 条 96 维 base 向量，10K 条 query，ground truth 为 top-100。所有正式本地实验取前 2000 条 query，$k=10$。报告中的平均延迟均为查询阶段平均延迟，不包含离线索引构建时间。

\subsection{结果文件与参数}

核心矩阵结果位于 \path{results/local/}，汇总文件为 \path{results/local_summary.csv}。补充 trade-off 结果为 \path{results/local_tradeoff.csv}。表 \ref{tab:coverage} 给出有效结果数量。

\begin{table}[H]
  \centering\small
  \begin{tabular}{lrrl}
    \toprule
    算法族 & 核心矩阵结果 & 补充 trade-off & 线程集合 \\
    \midrule
    Flat & 40 & 0 & 1,2,4,8,16 \\
    SQ & 32 & 0 & 1,4,8,16 \\
    PQ & 40 & 0 & 1,2,4,8,16 \\
    FastScan & 32 & 0 & 1,4,8,16 \\
    IVF & 45 & 5 & 1,2,4,8,16 \\
    IVF-PQ & 45 & 10 & 1,2,4,8,16 \\
    HNSW & 44 & 0 & 1,4,8,16 \\
    \midrule
    合计 & 278 & 15 & -- \\
    \bottomrule
  \end{tabular}
  \caption{本地实验结果覆盖范围}
  \label{tab:coverage}
\end{table}

核心矩阵的固定参数如下：
\begin{itemize}
  \item SQ/PQ 取 $p=100$，FastScan 取 $p=1000$；
  \item IVF 取 \texttt{nlist=16,nprobe=4}；
  \item IVF-PQ 取 \texttt{nlist=16,nprobe=4,p=100,mode=global}；
  \item HNSW 取 \texttt{ef=50}，IVF-HNSW 嵌套取 \texttt{nlist=16,nprobe=4}。
\end{itemize}

\section{实验结果与分析}

\subsection{核心矩阵最优结果}

表 \ref{tab:best} 汇总每个算法族的最优时延配置。加速比以同一算法族最快的 1 线程结果为基准，因此反映多线程扩展能力，而不是相对最朴素串行程序的总加速。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{llrrrr}
    \toprule
    算法族 & 最优变体 & 线程 & Recall@10 & 延迟 / $\mu$s & 相对最快 1 线程 \\
    \midrule
    Flat & \texttt{flat\_pthread\_dynamic\_inter} & 16 & 0.99995 & 298.95 & $10.67\times$ \\
    SQ & \texttt{sq\_pthread\_dynamic\_inter} & 16 & 0.99995 & 86.53 & $8.53\times$ \\
    PQ & \texttt{pq\_pthread\_dynamic\_inter} & 16 & 0.70860 & 73.06 & $9.37\times$ \\
    FastScan & \texttt{fastscan\_pthread\_dynamic\_inter} & 16 & 0.97525 & 125.81 & $10.51\times$ \\
    IVF & \texttt{ivf\_pthread\_pool\_inter} & 16 & 0.96225 & 104.67 & $7.25\times$ \\
    IVF-PQ & \texttt{ivfpq\_pthread\_dynamic\_inter} & 16 & 0.69180 & 27.44 & $8.71\times$ \\
    HNSW & \texttt{hnsw\_baseline} & 1 & 0.96865 & 65.48 & $1.00\times$ \\
    \bottomrule
  \end{tabular}
  \caption{各算法族核心矩阵最优点}
  \label{tab:best}
\end{table}

Flat 与 SQ 都能达到接近精确搜索的召回。PQ 和 IVF-PQ 在 $p=100$ 下速度很快但 recall 较低，说明 PQ 的候选数需要与召回目标联动调节。FastScan 在 $p=1000$ 下保持 0.97525 的 recall，并且 inter-query 多线程能把平均延迟压到 125.81 $\mu$s。IVF 的 recall 与速度介于 Flat 和 PQ 系列之间，\texttt{nprobe=4} 时达到 0.96225。

\subsection{Inter-query 与 Intra-query}

表 \ref{tab:granularity} 对比每个算法族最优的 inter-query 与 intra-query 结果。除 HNSW 外，所有扫描类算法的最佳结果均来自 inter-query。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{lrrrr}
    \toprule
    算法族 & 最优 inter 延迟 / $\mu$s & inter 线程 & 最优 intra 延迟 / $\mu$s & intra 线程 \\
    \midrule
    Flat & 298.95 & 16 & 1726.09 & 16 \\
    SQ & 86.53 & 16 & 704.31 & 4 \\
    PQ & 73.06 & 16 & 528.97 & 2 \\
    FastScan & 125.81 & 16 & 1322.76 & 1 \\
    IVF & 104.67 & 16 & 628.62 & 2 \\
    IVF-PQ & 27.44 & 16 & 410.31 & 4 \\
    \bottomrule
  \end{tabular}
  \caption{query 级并行与 query 内并行的最优结果对比}
  \label{tab:granularity}
\end{table}

原因主要有三点。第一，DEEP100K 实验使用 2000 条 query，query 级并行天然有足够任务数，线程间几乎没有共享状态。第二，intra-query 需要多个局部 top-$k$ 堆和最终 merge，固定开销在每条 query 上重复出现。第三，PQ、FastScan 和 IVF-PQ 的单 query 内部计算已经被量化压缩，继续切分会更快遇到 cache、内存带宽和同步瓶颈。

\subsection{Pthread 与 OpenMP 调度}

表 \ref{tab:scheduler} 给出 16 线程下若干代表性 inter-query 结果。Pthread dynamic 在多数算法中优于 OpenMP，Pthread pool 在 IVF 中表现最好。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{lrrrr}
    \toprule
    算法 & OMP inter & Pthread static & Pthread dynamic & Pthread pool \\
    \midrule
    Flat & 341.55 & 340.96 & \textbf{298.95} & 347.41 \\
    SQ & 118.30 & 126.12 & \textbf{86.53} & 92.42 \\
    PQ & 119.30 & 75.67 & \textbf{73.06} & 252.03 \\
    FastScan & 139.26 & 137.33 & \textbf{125.81} & 126.73 \\
    IVF & 113.90 & 132.08 & 116.94 & \textbf{104.67} \\
    IVF-PQ & 106.84 & 29.81 & \textbf{27.44} & 55.08 \\
    \bottomrule
  \end{tabular}
  \caption{16 线程 inter-query 下的调度策略对比，单位为 $\mu$s}
  \label{tab:scheduler}
\end{table}

Dynamic 的优势来自更好的尾部负载均衡。虽然每次抢任务需要原子操作，但 query 数量足够多，单个 query 的计算量也远大于一次 \texttt{fetch\_add}。IVF 的 pool 结果略优，说明 inverted list 扫描和索引对象访问使线程复用收益更明显。OpenMP 写法简洁，但在本机运行环境中调度常数和 runtime 行为不总是最优。

\subsection{Flat 线程伸缩性}

Flat 的 inter-query 线程伸缩性如表 \ref{tab:flat_scaling}。从 1 到 16 线程，三类 Pthread 变体均有明显加速，但 8 到 16 线程收益已经下降，说明全量扫描逐渐接近内存带宽上限。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{lrrrrr}
    \toprule
    变体 & 1 线程 & 2 线程 & 4 线程 & 8 线程 & 16 线程 \\
    \midrule
    OMP inter & 6769.48 & 1737.01 & 634.21 & 433.47 & 341.55 \\
    Pthread static inter & 5160.16 & 2214.15 & 675.36 & 436.45 & 340.96 \\
    Pthread dynamic inter & 3312.96 & 1407.33 & 563.03 & 388.78 & \textbf{298.95} \\
    Pthread pool inter & 3188.90 & 1542.36 & 578.00 & 398.82 & 347.41 \\
    \bottomrule
  \end{tabular}
  \caption{Flat inter-query 不同线程数延迟，单位为 $\mu$s}
  \label{tab:flat_scaling}
\end{table}

\subsection{IVF / IVF-PQ 的 Latency-Recall Trade-off}

表 \ref{tab:ivf_tradeoff} 展示 IVF 在 16 线程 Pthread pool inter-query 下调整 \texttt{nprobe} 的结果。随着 \texttt{nprobe} 增大，扫描簇数增加，recall 单调上升，延迟也总体上升。\texttt{nprobe=4} 是本地核心矩阵中使用的折中点，Recall@10 为 0.96225。

\begin{table}[H]
  \centering\small
  \begin{tabular}{rrrr}
    \toprule
    \texttt{nprobe} & Recall@10 & 延迟 / $\mu$s & 说明 \\
    \midrule
    1 & 0.73665 & 61.47 & 最低延迟，召回不足 \\
    2 & 0.87870 & 93.41 & 召回明显提升 \\
    4 & 0.96225 & 131.54 & 较好折中点 \\
    8 & 0.99430 & 210.35 & 接近精确召回 \\
    16 & 0.99995 & 455.89 & 几乎退化为更大范围扫描 \\
    \bottomrule
  \end{tabular}
  \caption{IVF 的 \texttt{nprobe} trade-off，\texttt{nlist=16}，16 线程}
  \label{tab:ivf_tradeoff}
\end{table}

表 \ref{tab:ivfpq_tradeoff} 对比 IVF-PQ 的 global-PQ-first 与 IVF-local-PQ 两种构建方式。Local-PQ 在本地数据上 recall 更高，尤其在低 \texttt{nprobe} 下优势明显，因为每个簇内单独量化能减少局部误差；但簇内 PQ 结构更复杂，构建与内存布局成本更高。Global-PQ-first 实现简单、存储统一，但在 $p=100$ 时 recall 上限明显较低。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{rrrrr}
    \toprule
    \texttt{nprobe} & Global recall & Global 延迟 / $\mu$s & Local recall & Local 延迟 / $\mu$s \\
    \midrule
    1 & 0.56240 & 49.73 & 0.65885 & 12.74 \\
    2 & 0.64840 & 59.39 & 0.76480 & 21.08 \\
    4 & 0.69180 & 85.08 & 0.82035 & 28.99 \\
    8 & 0.70725 & 47.24 & 0.83760 & 48.15 \\
    16 & 0.70870 & 81.43 & 0.84010 & 92.65 \\
    \bottomrule
  \end{tabular}
  \caption{IVF-PQ 两种构建方式的 \texttt{nprobe} trade-off，\texttt{p=100}，16 线程}
  \label{tab:ivfpq_tradeoff}
\end{table}

需要说明的是，IVF-PQ 的多线程结果存在一定运行噪声，尤其是在延迟已经降到几十微秒时，线程调度、缓存状态和系统后台负载都会造成非单调波动。因此表 \ref{tab:ivfpq_tradeoff} 更适合观察 recall 趋势和两种构建方式差异，而非把每一个延迟点解释为严格单调曲线。

\subsection{HNSW 图搜索}

HNSW 的结果见表 \ref{tab:hnsw}。baseline 最快，但 Recall@10 只有 0.96865。多入口点和边划分能提升召回，其中 \texttt{hnsw\_edge\_omp} 在 0.99915 recall 下延迟为 390.91 $\mu$s，\texttt{hnsw\_multi\_entry\_omp} 在 4 线程下达到 1.00000 recall，延迟为 464.85 $\mu$s。Layer0 点划分接近底层全扫描，召回高但延迟很大。

\begin{table}[H]
  \centering\scriptsize
  \begin{tabular}{lrrp{5.8cm}}
    \toprule
    变体 & 线程 & Recall@10 & 延迟 / $\mu$s 与分析 \\
    \midrule
    \texttt{hnsw\_baseline} & 1 & 0.96865 & 65.48，最快，但召回低于高精度要求 \\
    \texttt{hnsw\_ivf\_nested\_static} & 16 & 0.94705 & 221.90，速度较快但召回下降 \\
    \texttt{hnsw\_edge\_omp} & 1 & 0.99915 & 390.91，高召回代表点 \\
    \texttt{hnsw\_multi\_entry\_omp} & 4 & 1.00000 & 464.85，多入口点提高覆盖面 \\
    \texttt{hnsw\_layer0\_static} & 4 & 0.99995 & 3400.41，接近底层点划分扫描，代价过高 \\
    \bottomrule
  \end{tabular}
  \caption{HNSW 代表性结果}
  \label{tab:hnsw}
\end{table}

图搜索与向量扫描的差别在于搜索路径强依赖当前候选队列。线程越多，并不意味着能均匀拆分出更多独立工作。边划分在邻居数较小时无法摊薄同步成本，多入口点更像用额外计算换 recall，Layer0 点划分则牺牲了 HNSW 本身减少访问点的优势。因此 HNSW 的并行优化目标应是 recall-latency 折中，而不是单纯追求线程加速比。

\section{要求覆盖情况}

表 \ref{tab:req} 对照作业要求列出完成情况。

\begin{longtable}{p{4.1cm}p{9.2cm}}
  \toprule
  要求项 & 完成情况 \\
  \midrule
  Pthread 基础并行 & Flat、SQ、PQ、FastScan、IVF、IVF-PQ 均实现 static / dynamic / pool 的 inter 和 intra 版本；HNSW 实现 static / dynamic / pool 相关策略。 \\
  OpenMP 基础并行 & 对 Flat、SQ、PQ、FastScan、IVF、IVF-PQ 和 HNSW 主要策略实现 OpenMP 版本，编译使用 \texttt{-fopenmp}。 \\
  与 SIMD 结合 & 所有扫描类方法统一调用 \path{simd/} 下的 AVX2 / NEON 距离或查表内核。 \\
  Flat-SIMD 多线程 & 完成 query 级并行与 base 划分并行，比较不同线程数和调度方式。 \\
  PQ-SIMD 多线程 & 完成 PQ ADC 的 inter / intra 并行，分析 LUT 查表后 memory-bound 对扩展性的影响。 \\
  FastScan 进阶 & 实现 4-bit PQ FastScan 的 Pthread / OpenMP 并行版本，并给出补充矩阵。 \\
  IVF-SIMD baseline 与多线程 & 实现 IVF 索引、SIMD 搜索、Pthread / OpenMP 并行，并补充 \texttt{nprobe} trade-off。 \\
  IVF-PQ-SIMD baseline 与多线程 & 实现 IVF-PQ、两种构建模式、Pthread / OpenMP 并行，并比较 global/local 构建差异。 \\
  HNSW 进阶 & 基于 hnswlib 实现 baseline、多入口点、边划分、Layer0 点划分与 IVF-HNSW 嵌套结构。 \\
  性能测试与报告分析 & 本地收集 278 组核心矩阵结果和 15 组补充 trade-off 结果，分析线程数、调度、负载均衡、cache / 内存访问和负优化。 \\
  \bottomrule
  \caption{作业要求覆盖情况}
  \label{tab:req}
\end{longtable}

\section{总结}

本实验从单一 SIMD 内核扩展为完整 ANN 并行检索框架。最重要的结论是：在批量 query 场景中，query 级并行比单 query 内切分更稳健。它避免了局部 top-$k$ merge 和频繁同步，并能自然复用 SIMD 内核。Pthread dynamic 对尾部负载和系统调度噪声更宽容，线程池在 IVF 这类索引结构复用场景下也有优势。OpenMP 开发成本低，但 runtime 调度不一定总能取得最低延迟。

从算法角度看，Flat 提供高召回但受内存带宽限制；SQ 在本实验中取得近乎无损 recall 和较低延迟；PQ 与 IVF-PQ 速度很快但需要更大的 $p$ 或更好的构建方式提高 recall；FastScan 说明数据布局和查表方式会改变瓶颈；IVF 通过 \texttt{nprobe} 提供清晰的 latency-recall 旋钮；HNSW 则体现了图搜索并行化的挑战，内部并行更多是为高召回服务，而不是默认提升速度。

后续可以继续完善三点：第一，在鲲鹏平台用 \texttt{test.sh 2 1} 复跑完整矩阵，补齐 ARM NEON 与 x86 AVX2 的多线程对比；第二，对 SQ/PQ/FastScan 做更系统的 $p$ sweep，形成更完整的 latency-recall 曲线；第三，对 HNSW 尝试更细粒度的共享候选队列和无锁 visited 标记，验证是否能降低图搜索并行的同步成本。

\section*{AI 辅助说明}
\addcontentsline{toc}{section}{AI 辅助说明}

本项目使用 Codex 辅助完成代码结构梳理、实验脚本编排、结果汇总和报告一致性检查。所有性能数据均来自本地程序输出文件，未使用 AI 生成或伪造实验数据。关键结果可由 \path{results/local/}、\path{results/local_summary.csv} 和 \path{results/local_tradeoff.csv} 复核。

% ===========================================================================
% 实验上下文（供后续维护参考）
% ===========================================================================
%
% 实验参数:
%   数据集:    DEEP100K (d=96, N=100K, 10K queries, IP 距离)
%   评测 query: 前 2000 条
%   k:         10
%   SQ/PQ p:   100 (核心矩阵), FastScan p: 1000
%   IVF nlist: 16, nprobe: 4 (核心矩阵)
%   IVF-PQ:    nlist=16, nprobe=4, p=100, mode=global (核心矩阵)
%   HNSW ef:   50, IVF-HNSW nlist=16, nprobe=4
%
% 平台:
%   本机: i9-13900H (6P+8E/20T), Win11, MSYS2 GCC 14.2.0
%   编译: -O2 -mavx2 -mfma -fopenmp -lpthread -std=c++17 -I.
%   鲲鹏: 待跑 (bash test.sh 2 1)
%
% 数据文件来源:
%   本机: ../files/DEEP100K.{base.100k,query,gt.query.100k.top100}.{fbin,bin}
%   鲲鹏: /anndata/DEEP100K.{base.100k,query,gt.query.100k.top100}.{fbin,bin}
%
% 关键实验文件:
%   results/local/            — 278 组核心矩阵 + 15 组 trade-off 原始输出
%   results/local_summary.csv — 核心矩阵汇总
%   results/local_tradeoff.csv — nprobe/mode sweep 汇总
%
% 提交命令:
%   本实验序号为 2 (SIMD 为 1)，单节点:
%     bash test.sh 2 1
%
% 已知问题:
%   - IVF-PQ 低延迟区间 (~几十 us) 存在运行噪声，数据有非单调波动
%   - OMP 1 线程有明显 runtime 开销 (~2x vs pthread 1 线程)
%   - HNSW intra-query 并行基本为负优化（图搜索路径依赖强）
%   - 本机混合核 (P-core/E-core) 可能影响 8+ 线程的伸缩性
%
% 待完成:
%   - 鲲鹏 ARM NEON 全矩阵实验
%   - SQ/PQ/FastScan p-sweep 完整 latency-recall 曲线
%   - VTune / perf 分析 (cache miss, 内存带宽, 线程同步热点)
%   - 汇编对比 (x86 AVX2 vs ARM NEON 关键内核)
%
% ===========================================================================

\end{document}
