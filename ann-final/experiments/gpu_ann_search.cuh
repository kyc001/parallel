#pragma once

// Aggregation header kept for the original ANN coursework entry.
// The implementation is split under gpu/ in the same header-only style used by
// the previous ANN experiments' ivf/, omp/, pthread/, simd/ directories.

#include "gpu/common.cuh"
#include "gpu/gemm_search.cuh"
#include "gpu/ivf_search.cuh"
