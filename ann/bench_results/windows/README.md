# Windows Results

Reserved for benchmark outputs collected on Windows.

Use the reusable scripts in `../../benchmarks/` and write CSV/PNG/log files into this directory to avoid mixing results from different platforms.
