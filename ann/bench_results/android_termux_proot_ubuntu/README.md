# Android Termux PRoot Ubuntu Results

Platform: Android tablet, Termux + PRoot Ubuntu, ARM AArch64 with NEON `asimd` and `asimddp`.

This directory stores CSV, PNG, and log outputs for:

- E1 alignment comparison
- E2 software prefetch comparison
- E3 problem-size scaling
- E4 SQ/PQ latency-recall tradeoff figures
